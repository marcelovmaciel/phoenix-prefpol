# Full Public API

```@meta
CurrentModule = Preferences
```

This page keeps the complete exported API discoverable without duplicating the
canonical docstring panels rendered on the conceptual pages.

```@index
Modules = [Preferences]
```
